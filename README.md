Switch Case

O switch case é uma estrutura de decisão que permite executar diferentes blocos de código de acordo com o valor de uma variável ou expressão. Cada case representa uma possível opção, e o default é executado quando nenhuma das opções corresponde ao valor informado.
